/* Ensemble des tests sur les variables (hors type custom / array) */
let
    var a :int := 3
    var b := "test"
    var d:string := 3
    var e := for i:= 0 to 10 do (3)
    var a := 5
    var q := break
in  
    a + c;
    a + b;
    b:= 5
end

/* 8 errors */