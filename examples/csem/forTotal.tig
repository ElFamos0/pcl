/* Quelques tests de contrôles sur le for*/
let
    var a:= 3
in  
  for i:= 0 to for j:= 0 to 10 do (3) do (3);
  for i:= for j:= 0 to 10 do (3) to 10 do (3);
  for i:= 0 to "test" do (3);
  for i:= "test" to 10 do (3);
  for a:= 0 to 10 do (3)
end