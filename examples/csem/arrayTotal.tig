/* Quelques tests de contrôles à faire sur les array */
let
    type newArrayType = array of int
    var a:= newArrayType [10] of 0
    var b:= 5
    var d:= newArrayType [10] of for i:=0 to 10 do (3)
    var e:= newArrayType [10] of "test"
    var f:= newArrayType [for i:= 0 to 10 do (3)] of 0
    var g:= newArrayType ["test"] of 0
    var h:= int [10] of 0
in  
    a[for i:= 0 to 10 do (3)];
    b[2];
    a["test"];
    a[-5]
end

/* 8 errors */