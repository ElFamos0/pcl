let
    var x := 4
    var y := 2
    function f(x:int,y:int) = (
        print(x);
        print(y);
        print(x/y)
    )
    function g(x:int,y:int) : int = x*y
in
    while x > 0 do (
        x := x - 1;
        print(x);
        if x < 2
        then break
    )
end