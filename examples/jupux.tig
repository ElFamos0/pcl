let
    var x := 4
    var y := 2
    function f(x:int,y:int) : int = x*y
    function g(x:int,y:int) : int = x/y
in
    while x > 0 do (x:= x-1; y := y*2);
    print(y);
    if x = 0 then print(1) else print(0);
    if x <> 0 then print(1) else print(0);
    for i:= 2 to 10 do print(i)
end