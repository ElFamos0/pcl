let
    function fibo(n:int):int = (
        print(n);
        print("\n");
        if n <= 1 then (
            n
        ) else (
            fibo(n-1)+fibo(n-2)
        )
    )
in
    print(fibo(5));
    print("\n")
end