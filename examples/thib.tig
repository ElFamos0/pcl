let
    var N := 8

    var y := let
        function f(n: int): int = (
            print(N);
            print(n);
            if n > 0
            then f(n - 1)
            else (
                for i := 0 to 5 do (
                    print(i)
                );
                1
            )
        )
    in
        f(4)
    end
in
    print(y)
end